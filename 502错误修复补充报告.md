# 502错误补充修复报告

## 问题现象

访问 `http://120.77.222.102:3000/admin` 出现502错误：
```
该网页无法正常运作
120.77.222.102 目前无法处理此请求。
HTTP ERROR 502
```

## 根本原因分析（基于Systematic Debugging Phase 1）

### 证据收集

1. **容器状态**
   - my-web容器正常运行
   - Next.js启动成功

2. **nginx配置检查**
   ```
   upstream 120.77.222.102 {
       # Cannot connect to network 'app-network' of this container
       # Fallback entry
       server 127.0.0.1 down;
   }
   ```

3. **网络连接分析**
   - `nginx-proxy` 网络：redis, postgres, nginx-proxy, my-server, nginx-proxy-acme
   - `app-network` 网络：redis, postgres, my-web

4. **Next.js监听地址**
   ```
   tcp  0  0 172.19.0.4:3000  0.0.0.0:*  LISTEN
   ```
   Next.js只监听在 `172.19.0.4:3000`（app-network的IP），而不是 `0.0.0.0:3000`

## 根本原因

**双重问题：**

1. **网络隔离问题**
   - my-web连接到 `app-network`
   - nginx-proxy使用 `nginx-proxy` 网络
   - 两个网络不互通，导致nginx无法路由请求

2. **Next.js监听地址问题**
   - Next.js只监听在app-network的IP地址
   - 即使连接到同一网络，也无法从其他网络接口访问

## 修复步骤

### 修复1：设置Next.js监听所有接口

**原理：** Next.js standalone模式在启动时会读取HOSTNAME环境变量，如果设置为 `0.0.0.0`，则会监听所有网络接口。

**命令：**
```bash
docker run -d --name my-web \
  --network app-network \
  --expose 3000 \
  -e VIRTUAL_HOST=kxpwty.cn,www.kxpwty.cn,120.77.222.102 \
  -e LETSENCRYPT_HOST=kxpwty.cn,www.kxpwty.cn \
  -e VIRTUAL_PORT=3000 \
  -e HOSTNAME=0.0.0.0 \
  my-web-fixed:v1
```

**结果：**
```
▲ Next.js 14.2.35
- Local:        http://localhost:3000
- Network:      http://0.0.0.0:3000  ← 成功监听所有接口
```

### 修复2：连接到nginx-proxy网络

**命令：**
```bash
docker network connect nginx-proxy my-web
docker restart nginx-proxy
```

**结果：**
nginx配置更新为：
```
upstream 120.77.222.102 {
    ## Can be connected with "nginx-proxy" network
    # my-web
    server 172.18.0.5:3000;
}
```

## 验证结果

### 测试1：内部连接测试

```bash
docker exec nginx-proxy wget -qO- http://my-web:3000/admin
```

✅ 返回完整HTML页面

### 测试2：外部访问测试

```bash
curl -I -H 'Host: 120.77.222.102' http://localhost/admin
```

**结果：**
```
HTTP/1.1 307 Temporary Redirect
Location: http://120.77.222.102/admin/login?redirect=%2Fadmin
```

✅ 正确重定向到登录页

## 技术细节

### 为什么设置HOSTNAME=0.0.0.0有效？

Next.js standalone模式的server.js启动逻辑：

```javascript
const hostname = process.env.HOSTNAME || os.hostname();
server.listen(port, hostname);
```

- 如果不设置HOSTNAME：使用 `os.hostname()`（容器ID）
- 如果设置HOSTNAME=公网IP：尝试监听不存在的IP，启动失败
- 如果设置HOSTNAME=0.0.0.0：监听所有网络接口，可以接受来自任何网络的请求

### 为什么需要连接两个网络？

1. **nginx-proxy** 需要通过这个网络发现容器并生成upstream配置
2. 容器需要能从nginx-proxy网络访问到

## 总结

**问题状态：** ✅ 已修复

**关键修复点：**
1. ✅ 设置 `HOSTNAME=0.0.0.0` 让Next.js监听所有接口
2. ✅ 将my-web连接到nginx-proxy网络
3. ✅ 重启nginx-proxy刷新配置

**最终验证：**
- 内部测试：成功返回HTML
- 外部测试：正确307重定向

---

**报告生成时间：** 2026-08-01
**问题状态：** 已完全解决