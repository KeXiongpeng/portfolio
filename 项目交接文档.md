# Portfolio 项目交接文档

> 更新时间：2026-08-01

---

## 一、项目概述

个人作品集网站，包含前台展示 + 管理后台，前后端分离部署。

| 组件 | 技术栈 | 端口 |
|------|--------|------|
| 前端（Web） | Next.js 14 + Ant Design 5 | 3000 |
| 后端（Server） | NestJS + TypeORM + PostgreSQL | 3001 |
| 数据库 | PostgreSQL 15 | 5432（容器内部） |
| 缓存 | Redis 7 | 6379（容器内部） |
| 反向代理 | nginx-proxy（自动 SSL） | 80/443 |

---

## 二、服务器信息

| 项目 | 值 |
|------|-----|
| 服务器 IP | `120.77.222.102` |
| 域名 | `kxpwty.cn` / `www.kxpwty.cn` |
| API 域名 | `api.kxpwty.cn` |
| SSH 用户 | `root` |
| SSH 密码 | `Wtd021219` |
| 管理后台账号 | `kexiongpeng` / `Wtd021219` |

### 当前访问地址（备案通过前，IP 直连）

- 前台首页：`http://120.77.222.102:3000`
- 管理后台：`http://120.77.222.102:3000/admin`
- 后端 API：`http://120.77.222.102:3001`

### 备案通过后访问地址

- 前台：`https://kxpwty.cn`
- 管理后台：`https://kxpwty.cn/admin`
- 后端 API：`https://api.kxpwty.cn`

---

## 三、Docker 容器清单

```
# 在服务器上运行查看
docker ps --format "table {{.Names}}\t{{.Image}}\t{{.Ports}}\t{{.Status}}"
```

| 容器名 | 镜像 | 说明 |
|--------|------|------|
| `my-web` | `my-project-portfolio/web:v1.1.2` | 前端 Next.js |
| `my-server` | `my-project-portfolio/server:latest` | 后端 NestJS |
| `postgres` | `postgres:15-alpine` | PostgreSQL 数据库 |
| `redis` | `redis:7-alpine` | Redis 缓存 |
| `nginx-proxy` | `jwilder/nginx-proxy` | 反向代理 + 自动 HTTPS |

### 所有容器在同一 Docker 网络

```
nginx-proxy  # 三个自定义容器都在这个网络中
```

---

## 四、当前唯一未修复的问题（重要）

### 问题描述

前端 v1.1.2 镜像构建时 `NEXT_PUBLIC_API_URL` 为空，导致浏览器中前端 JS 的 API 请求指向 `http://localhost:3001`（用户自己电脑），而非服务器。

**现象：** 登录页能打开，但点击登录后 502 报错；Dashboard 无数据。

### 修复方案（3 步）

#### 步骤 1：本地重新构建前端镜像

```powershell
cd C:\Users\sun\Desktop\学习产出\web

# 注意：必须加 --no-cache，否则 Docker 用缓存导致环境变量仍然为空
docker build --no-cache `
  --build-arg NEXT_PUBLIC_API_URL=http://120.77.222.102:3001 `
  --build-arg NEXT_PUBLIC_SITE_URL=http://120.77.222.102:3000 `
  -t my-project-portfolio/web:v1.1.3 .
```

#### 步骤 2：导出并上传到服务器

```powershell
# 导出 tar
docker save my-project-portfolio/web:v1.1.3 -o C:\Users\sun\Desktop\web-v1.1.3.tar

# 上传（需要输入密码 Wtd021219）
scp C:\Users\sun\Desktop\web-v1.1.3.tar root@120.77.222.102:/root/
```

#### 步骤 3：服务器上加载镜像并重建容器

```bash
# SSH 登录后执行
docker load -i /root/web-v1.1.3.tar
docker stop my-web && docker rm my-web
docker run -d --name my-web --restart unless-stopped \
  --network nginx-proxy -p 3000:3000 \
  -e VIRTUAL_HOST=kxpwty.cn,www.kxpwty.cn \
  -e LETSENCRYPT_HOST=kxpwty.cn,www.kxpwty.cn \
  -e VIRTUAL_PORT=3000 \
  -e NODE_ENV=production \
  my-project-portfolio/web:v1.1.3
docker restart nginx-proxy
```

#### 验证

1. 浏览器打开 `http://120.77.222.102:3000/admin` → 登录页
2. 输入 `kexiongpeng` / `Wtd021219` → 登录成功跳转到 dashboard
3. 打开 `http://120.77.222.102:3000` → 首页显示数据

---

## 五、备案通过后的操作

备案通过后需要重新构建前端镜像，将 IP 替换为域名：

```powershell
cd C:\Users\sun\Desktop\学习产出\web

docker build --no-cache `
  --build-arg NEXT_PUBLIC_API_URL=https://api.kxpwty.cn `
  --build-arg NEXT_PUBLIC_SITE_URL=https://kxpwty.cn `
  -t my-project-portfolio/web:v2.0.0 .
```

然后重复「步骤 2 + 步骤 3」上传部署即可。同时需要在阿里云 DNS 解析中配置：

| 类型 | 主机记录 | 记录值 |
|------|----------|--------|
| A | `@` | `120.77.222.102` |
| A | `www` | `120.77.222.102` |
| A | `api` | `120.77.222.102` |

nginx-proxy 会自动申请 Let's Encrypt SSL 证书（通过容器的 `LETSENCRYPT_HOST` 环境变量）。

---

## 六、项目目录结构

```
C:\Users\sun\Desktop\学习产出\
├── web\                    # 前端 Next.js 项目
│   ├── app\               # App Router 页面
│   │   ├── admin\         # 管理后台（登录、Dashboard、博客、项目、Profile、统计）
│   │   ├── blog\          # 前台博客页
│   │   ├── projects\      # 前台项目页
│   │   ├── api\           # Next.js API Routes（set-cookie、clear-cookie）
│   │   └── layout.tsx     # 根布局
│   ├── components\        # 组件
│   ├── lib\               # 工具库（api.ts、types.ts）
│   ├── Dockerfile         # 前端镜像构建
│   ├── .dockerignore      # Docker 忽略文件
│   └── next.config.js     # Next.js 配置
│
└── server\                 # 后端 NestJS 项目
    ├── src\modules\       # 业务模块
    │   ├── auth\          # 认证（登录/注册/GitHub OAuth）
    │   ├── blog\          # 博客管理
    │   ├── project\       # 项目管理
    │   ├── profile\       # 个人资料
    │   ├── contact\       # 联系消息
    │   ├── upload\        # 文件上传
    │   └── visit\         # 访客统计
    ├── Dockerfile         # 后端镜像构建
    ├── .env               # 后端环境变量（数据库/Redis/JWT密钥）
    └── package.json
```

---

## 七、关键环境变量说明

### 前端（构建时注入，烘焙到 JS 中）

| 变量 | 备案前 | 备案后 |
|------|--------|--------|
| `NEXT_PUBLIC_API_URL` | `http://120.77.222.102:3001` | `https://api.kxpwty.cn` |
| `NEXT_PUBLIC_SITE_URL` | `http://120.77.222.102:3000` | `https://kxpwty.cn` |

### 后端（运行时读取 .env）

| 变量 | 说明 |
|------|------|
| `DATABASE_URL` | PostgreSQL 连接串 |
| `REDIS_URL` | Redis 连接串 |
| `JWT_SECRET` | JWT 签名密钥 |
| `COOKIE_DOMAIN` | Cookie 作用域（备案后改为 `.kxpwty.cn`） |

### 后端 Cookie 配置（关键）

登录时 set-cookie 路由当前设置了 `secure: false`。备案通过切换 HTTPS 后，需要改为 `secure: true`。

修改位置：`server/src/modules/auth/auth.service.ts` 中的 `setCookie` 方法。

---

## 八、管理后台功能清单

| 功能 | 路径 | 说明 |
|------|------|------|
| 仪表盘 | `/admin/dashboard` | 统计概览（博客数、项目数、访客数） |
| 博客管理 | `/admin/blogs` | 增删改查、发布/取消发布 |
| 项目管理 | `/admin/projects` | 增删改查、拖拽排序 |
| 个人信息 | `/admin/profile` | 编辑姓名/简介/社交链接、上传头像 |
| 访客统计 | `/admin/analytics` | PV/UV 统计、来源分析 |

---

## 九、常用运维命令

```bash
# 查看所有容器状态
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

# 查看前端日志
docker logs my-web --tail 50 -f

# 查看后端日志
docker logs my-server --tail 50 -f

# 重启某个容器
docker restart my-web

# 查看数据库
docker exec -it postgres psql -U postgres -d portfolio

# 查看防火墙端口
firewall-cmd --list-ports

# 开放新端口
firewall-cmd --permanent --add-port=端口号/tcp && firewall-cmd --reload
```

---

## 十、已踩过的坑（避免重复踩）

| # | 问题 | 根因 | 解决 |
|---|------|------|------|
| 1 | 前端白屏/卡住 | `.dockerignore` 未排除 `.env.local`，构建时覆盖了 build args | `.dockerignore` 加 `.env.local` |
| 2 | 登录后跳回登录页 | cookie `secure: true` 在 HTTP 下被浏览器丢弃 | 改为 `secure: false` |
| 3 | 外部访问 3000 端口 404 | 服务器 firewalld 未开放端口 | `firewall-cmd --add-port=3000/tcp` |
| 4 | 登录 502 报错 | `NEXT_PUBLIC_API_URL` 为空，前端请求发到 localhost | 构建时传入正确的 build arg |
| 5 | `--no-cache` 不加 | Docker 用缓存，build arg 变了但没生效 | 必须 `docker build --no-cache` |
| 6 | SCP 需要密码 | SSH 密钥认证被阿里云安全加固拦截 | 用密码手动输入 | 
