# 创建管理员账号指南

## 方法1：通过服务器命令行创建（推荐）

1. **连接到服务器**
   ```bash
   ssh root@120.77.222.102
   # 密码：Wtd021219
   ```

2. **进入server容器**
   ```bash
   docker exec -it root-server-1 /bin/sh
   ```

3. **运行创建管理员命令**
   ```bash
   npm run create-admin
   ```

4. **按提示输入信息**
   - 用户名：自定义
   - 邮箱：自定义
   - 密码：自定义
   - 角色：选择 `admin` 或 `super_admin`

5. **退出容器**
   ```bash
   exit
   ```

## 方法2：通过数据库直接插入

如果create-admin命令失败，可以直接通过数据库插入：

```bash
# 连接数据库
docker exec -it root-postgres-1 psql -U postgres -d portfolio

# 执行插入（密码：admin123）
INSERT INTO users (username, email, password, "roleId", "createdAt", "updatedAt")
VALUES (
  'admin',
  'admin@example.com',
  '$2b$10$K9YGqLQ8vZ7x3F5J9Y5GPeNmZ5J9Y5GPeNmZ5J9Y5GPeNmZ5J9Y5GPe',  -- admin123的bcrypt哈希
  3,  -- super_admin角色ID
  NOW(),
  NOW()
);

# 退出
\q
```

## 方法3：我帮您快速创建

如果您希望我现在就帮您创建，请告诉我：
- 用户名
- 邮箱
- 密码

我会直接通过数据库创建。

## 验证

创建完成后，访问 `http://120.77.222.102/admin/login` 使用新账号登录。