# 官网项目需求文档

> **Workspace:** c:\Users\sun\myapp

---

**User**

Use Skill: using-superpowers 这个月我在我师父的带领之下学习了 前端：nodejs npm nextjs App router seo（sitemap） geo robots .txt  React Vercel  tailwindcss 响应式布局  后端docker容器 redis postgresql OAuth2 jwt鉴权 nestjs vite arcodesign antdesign这些技术栈 现在我就是要拿着这些首先来制作一个官网类的项目 但是我目前不知道需要什么需求 现在假设你是一个客户你来找我制作这个项目并且提出相对应的需求 给出需求文档  目的是包含这些知识点 后续我会将他部署到线上让他成为一个可以使用的完整的项目  你可以上github上模仿一下别人的项目

**Assistant**

Using brainstorming skill to explore requirements for this project.

Let me first explore the current project context.

好的，我看到你当前项目是一个基于 Umi Max + Ant Design Pro 的后台管理系统。但你的技术栈列表里提到的是 **Next.js App Router**，这意味着官网项目会是一个全新的项目，而不是在当前项目上修改。

在你列出的技术栈中，我注意到有些技术组合需要注意：

- **Next.js App Router**（前端框架）+ **Vite**（构建工具）—— 这两者其实是有冲突的，Next.js 自带构建系统，Vite 通常不与 Next.js 一起使用
- **Arcodesign** 和 **Ant Design** —— 这是两个不同的 UI 库

我的第一个问题：



好的，个人简历/作品集网站。让我先上 GitHub 看看优秀的个人简历网站项目，学习一下常见的设计模式和功能。

我已经调研了 GitHub 上大量优秀的个人简历/作品集网站项目。现在我注意到一个关键问题——你提到要覆盖 **NestJS、Docker、Redis、PostgreSQL、OAuth2、JWT 鉴权** 这些后端技术，但普通的个人简历网站通常是纯静态的，不需要后端。

好的，明确了：**个人简历网站 + 管理后台 + 博客系统 + 访客统计**，覆盖前后端全栈技术。下一个问题：

好，需求已经清晰了。现在提出架构方案供你选择：

## 方案对比

### 方案 A：Monorepo 单仓库（推荐）

前端（Next.js）和后端（NestJS）放在同一个 Git 仓库的不同目录下（如 `web/` 和 `server/`），用 Docker Compose 统一编排。

**优势：** 一次 git clone 拿到全部代码；部署简单，一个 docker-compose up 全部启动；前后端类型可以共享。
**劣势：** 仓库稍大，需要配置 monorepo 工具。

### 方案 B：双仓库分离

前端和后端各一个独立仓库，通过 API 通信。

**优势：** 职责完全分离，可以独立部署。
**劣势：** 需要维护两个仓库；部署时要分别操作；类型不能直接共享。

### 方案 C：Next.js 全栈

不用 NestJS，后端逻辑全部写在 Next.js 的 Route Handlers / Server Actions 中。

**优势：** 只有一个项目，最简单。
**劣势：** **无法覆盖 NestJS 技术点**，与你的学习目标冲突。

**我推荐方案 A**，原因：既满足你学 NestJS 的需求，又让部署和管理最方便。

好，现在逐部分呈现设计方案。

---

## 第一部分：项目概览与前台页面结构

**项目名称：** `portfolio`（个人作品集与博客网站）

**前台页面（公开访问）：**

| 页面 | 路由 | 说明 |
|------|------|------|
| 首页 | `/` | Hero 区（姓名、职位、一句话介绍）+ 技能展示 + 精选项目 + 最新博客 + 访客计数器 |
| 关于我 | `/about` | 详细个人介绍、工作经历时间线、教育背景 |
| 项目作品 | `/projects` | 项目列表，支持按技术栈标签筛选 |
| 项目详情 | `/projects/[slug]` | 单个项目详情页，截图、描述、技术栈、链接 |
| 博客列表 | `/blog` | 文章列表，分页，按标签/分类筛选 |
| 博客详情 | `/blog/[slug]` | MDX 渲染的文章页，代码高亮，目录导航 |
| 联系我 | `/contact` | 联系表单（发送到后端 API，存入数据库） |

**设计风格：** 极简科技风，深色/浅色主题切换，大量留白，平滑滚动动画。

这部分设计你觉得可以吗？有需要调整的吗？

## 第二部分：管理后台页面结构

**后台页面（需 GitHub OAuth 登录，`/admin` 路径下）：**

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录 | `/admin/login` | GitHub OAuth2 登录入口，登录成功跳转后台 |
| 仪表盘 | `/admin/dashboard` | 总览面板：访客统计（总 PV、今日 PV、在线人数）、博客数量、项目数量 |
| 博客管理 | `/admin/blogs` | 文章列表（标题、状态、日期），支持新建/编辑/删除/发布/下线 |
| 博客编辑 | `/admin/blogs/[id]/edit` | MDX Markdown 编辑器，标题/标签/摘要/内容，实时预览 |
| 项目管理 | `/admin/projects` | 项目 CRUD，上传封面图，设置技术栈标签、链接 |
| 个人信息 | `/admin/profile` | 编辑首页展示的个人信息（姓名、职位、简介、社交链接等） |
| 访客统计 | `/admin/analytics` | 基础统计面板：总访问量、今日/本周/本月 PV、在线人数，简单折线图 |

**权限控制：** 只有站长（通过 GitHub OAuth 登录且 GitHub ID 在白名单中）才能进入后台。JWT token 存储在 HttpOnly Cookie 中，前端请求时自动携带。

## 第三部分：技术架构与数据模型

**整体架构：**

```
用户浏览器
  ├── 前台（Next.js App Router）──→ Vercel 部署
  └── 后台（Next.js App Router + Ant Design）──→ Vercel 部署
         │
         └── API 请求 ──→ NestJS 后端 ──→ 服务器 Docker 部署
                              ├── PostgreSQL（数据持久化）
                              └── Redis（缓存 + 访客计数）
```

**数据库表设计（PostgreSQL）：**

- `users` — 管理员用户（github_id, username, avatar, role）
- `projects` — 项目作品（title, slug, description, cover_url, tech_stack, link, github_url, sort_order）
- `blogs` — 博客文章（title, slug, content(mdx), summary, tags, status[draft/published], published_at）
- `contacts` — 联系表单提交（name, email, message, created_at）
- `visit_stats` — 访问统计记录（date, pv, uv）

**Redis 用途：**
- 访客 PV/UV 计数器（INCR，定期持久化到 PostgreSQL）
- 博客列表缓存（避免每次请求都查数据库）
- GitHub OAuth state 参数临时存储（防 CSRF）

**Docker 编排：**
- `web` — Next.js 前端（开发时用，生产用 Vercel）
- `server` — NestJS 后端
- `postgres` — PostgreSQL 16
- `redis` — Redis 7

## 第四部分：技术栈覆盖清单与 SEO 策略

**技术栈覆盖映射：**

| 你学过的技术 | 在项目中的使用方式 |
|---|---|
| Node.js / npm | 项目包管理，NestJS 运行时 |
| Next.js App Router | 前台所有页面 + 后台页面 |
| SEO（sitemap） | `app/sitemap.ts` 自动生成全站 sitemap |
| Geo（生成式引擎优化 GEO） | 结构化数据（JSON-LD / schema.org）、llms.txt、语义化 HTML，让 ChatGPT/Perplexity 等 AI 搜索引擎更容易提取、理解和引用网站内容 |
| robots.txt | `app/robots.ts` 配置，允许前台抓取，禁止 `/admin` |
| React | 所有 UI 组件 |
| Vercel | 前端生产部署 |
| Tailwind CSS | 前台样式，响应式布局 |
| 响应式布局 | 所有页面移动端适配（sm/md/lg/xl） |
| Docker | 后端 + PostgreSQL + Redis 容器化，docker-compose 编排 |
| Redis | 访客计数缓存 + 博客列表缓存 + OAuth state |
| PostgreSQL | 所有业务数据持久化 |
| OAuth2 | GitHub 第三方登录 |
| JWT 鉴权 | 登录后签发 JWT，HttpOnly Cookie 存储，中间件校验 |
| NestJS | 后端 RESTful API |
| Ant Design | 管理后台 UI 组件库 |

**SEO 策略：**
- 每个页面设置 `metadata`（title、description、openGraph）
- 自动生成 `sitemap.xml`
- `robots.txt` 放行前台页面，屏蔽 `/admin/*` 和 `/api/*`
- 博客文章支持 canonical URL
- Next.js Image 组件优化图片加载

整体设计到这里就全部呈现完了。

 I apologize - let me clean up and properly create the directory.

---

