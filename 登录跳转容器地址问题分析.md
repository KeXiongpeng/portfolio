# 登录跳转容器地址问题分析报告

## 📋 问题概述

**问题现象：**
用户访问管理后台时，登录成功后会跳转到容器内部地址（`http://110fdc8ddfdb:3000`），导致页面无法访问（502错误）。

**影响范围：**
- 访问 `http://120.77.222.102:3000/admin` 登录后跳转到容器地址
- 访问 `http://120.77.222.102:3000/admin/login` 无需登录即可进入后台
- 退出登录时重定向到容器地址

**用户测试环境：**
- PC浏览器（Chrome无痕模式）
- 手机浏览器
- 问题在两个环境下都存在

---

## 🔍 问题分析

### 根本原因

**Next.js在容器环境下错误地使用了容器的hostname（容器ID）来构建绝对URL。**

#### 详细分析：

1. **容器hostname问题**
   - Docker容器默认hostname为容器ID（如 `110fdc8ddfdb`）
   - Next.js standalone模式在处理请求时，使用了 `os.hostname()` 或 `req.headers.host` 来构建URL
   - 当用户通过公网IP访问时，Next.js仍然使用容器hostname生成重定向URL

2. **环境变量配置问题**
   - `NEXT_PUBLIC_*` 变量在构建时硬编码到JavaScript中
   - 如果构建时使用了错误的URL，会导致前端代码中包含错误的地址
   - 运行时环境变量（如 `HOSTNAME`）需要正确配置

3. **Middleware重定向逻辑**
   - Middleware在重定向时使用了 `req.nextUrl.clone()`，可能会继承容器hostname
   - 需要强制使用请求头中的正确主机名

---

## 🛠️ 已实施的修复方案

### 1. 修改登录跳转逻辑（关键修复）

**文件：** `web/app/admin/login/page.tsx`

```typescript
// 修复前：使用 router.push 可能导致hostname问题
router.push(redirect);

// 修复后：使用 window.location.href 确保使用当前页面的origin
window.location.href = redirect;
```

**原因：** `router.push()` 可能被Next.js内部逻辑处理，使用错误的hostname。`window.location.href` 是浏览器原生跳转，会使用当前页面的origin。

---

### 2. 修改Middleware重定向逻辑

**文件：** `web/middleware.ts`

```typescript
// 强制使用请求头中的正确主机名
const correctHost = req.headers.get('x-forwarded-host') || req.headers.get('host');
if (correctHost && !/^[a-f0-9]{12}:/i.test(correctHost)) {
  loginUrl.host = correctHost.split(':')[0];
  loginUrl.port = correctHost.split(':')[1] || '';
}
```

**作用：** 确保middleware重定向时使用正确的主机名，而不是容器hostname。

---

### 3. 配置Next.js信任Host头

**文件：** `web/next.config.js`

```javascript
experimental: {
  // 确保所有路由都使用相对路径
  trustHostHeader: true,
},
```

**作用：** 配置Next.js信任请求头中的主机名。

---

### 4. 更新环境变量配置

**文件：** `docker-compose.yml`

```yaml
web:
  build:
    args:
      - NEXT_PUBLIC_API_URL=http://120.77.222.102:3001
      - NEXT_PUBLIC_SITE_URL=http://120.77.222.102:3000
  environment:
    - VIRTUAL_HOST=kxpwty.cn,www.kxpwty.cn,120.77.222.102
    - HOSTNAME=120.77.222.102
```

**作用：** 确保构建时和运行时都使用正确的URL。

---

## 📊 测试结果对比

### 我的测试环境（本地Chrome无痕模式）
- ✅ 登录后跳转到：`http://120.77.222.102:3000/admin/dashboard`（正确）
- ✅ 访问 `/admin/login` 需要登录（正确）
- ✅ 退出登录正常（正确）
- ❌ 未出现容器地址

### 用户测试环境（手机浏览器）
- ❌ 登录后跳转到容器地址
- ❌ `/admin/login` 不需要登录
- ❌ 退出登录跳转到容器地址

---

## 🚨 疑点和未解决问题

### 关键疑点：

1. **测试结果不一致**
   - 我的测试显示问题已修复
   - 用户的测试显示问题依然存在
   - **可能原因：** 测试环境差异、网络环境差异、DNS解析差异

2. **`/admin/login` 不需要登录的问题**
   - 我的测试：新无痕窗口访问需要登录
   - 用户测试：不需要登录就能进入
   - **可能原因：** 浏览器缓存了登录态、Cookie未清除、或者存在其他认证逻辑问题

3. **为什么我的测试正常？**
   - 可能我测试的是修复后的版本
   - 可能我的网络环境不同
   - 可能存在其他未知因素

---

## 🎯 建议的下一步行动

### 1. 立即检查（高优先级）

```bash
# 连接服务器检查容器状态
docker ps --format "table {{.ID}}\t{{.Names}}\t{{.Status}}"

# 检查容器的hostname
docker exec web hostname

# 检查容器环境变量
docker exec web env | grep -i "host\|url"

# 检查nginx配置
docker exec nginx cat /etc/nginx/conf.d/default.conf
```

### 2. 彻底重置方案

如果问题依然存在，建议执行以下操作：

```bash
# 1. 停止所有容器
docker-compose down

# 2. 清理旧镜像
docker rmi $(docker images -q web) 2>/dev/null

# 3. 无缓存重新构建
docker-compose build --no-cache web

# 4. 启动服务
docker-compose up -d

# 5. 查看日志
docker-compose logs -f web
```

### 3. 检查DNS和代理

- 确认域名解析是否正确
- 检查是否有代理服务器
- 验证nginx-proxy配置

### 4. 添加更多日志

在以下位置添加详细日志：
- Middleware（记录每个请求的hostname）
- 登录API（记录重定向URL）
- 前端登录逻辑（记录跳转URL）

---

## 💡 深层原因分析

### Next.js standalone模式的已知问题

Next.js standalone模式在容器环境下存在hostname问题：

1. **URL生成机制**
   - Next.js在某些情况下会使用 `os.hostname()` 生成URL
   - 在容器中，这会返回容器ID

2. **解决方案**
   - 使用 `trustHostHeader: true` 配置
   - 强制使用 `window.location.href` 而不是 `router.push()`
   - 在middleware中显式设置正确的主机名

### 环境变量的影响

1. **构建时变量**
   - `NEXT_PUBLIC_*` 变量在构建时硬编码
   - 需要确保构建时使用正确的值

2. **运行时变量**
   - `HOSTNAME` 等变量在容器启动时设置
   - 需要通过docker-compose正确配置

---

## 📝 总结

### 问题状态
- **部分修复：** 我的测试环境显示问题已解决
- **用户环境：** 问题依然存在

### 关键修复点
1. ✅ 登录跳转使用 `window.location.href`
2. ✅ Middleware强制使用正确的主机名
3. ✅ Next.js配置信任Host头
4. ✅ 环境变量配置正确

### 需要进一步排查
1. 🔍 服务器端实际运行状态
2. 🔍 用户的网络环境
3. 🔍 是否存在其他中间件或代理
4. 🔍 `/admin/login` 为什么不需要登录的真正原因

---

## 📞 联系信息

如果在新的会话中继续排查，请提供：
1. 服务器上执行 `docker exec web hostname` 的输出
2. 服务器上执行 `docker exec web env | grep HOST` 的输出
3. 用户访问时的完整URL变化过程
4. 是否使用了代理或VPN

---

**报告生成时间：** 2026-08-01
**问题状态：** 部分修复，需要进一步排查
**优先级：** 高